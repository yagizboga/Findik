using UnityEngine;

public class IngredientTypes : MonoBehaviour
{
    public enum IngredientType 
    {
        Bread, Marul, Meat, Tomato // Order is important, do NOT change. Add new ingredients to the end of the order.
    }

}
